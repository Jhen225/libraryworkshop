import { SortByLastPipe } from './sort-by-last.pipe';

describe('SortByFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByLastPipe();
    expect(pipe).toBeTruthy();
  });
});
